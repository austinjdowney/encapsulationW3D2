let str = "What's the longest word in this sentence"


function longestWord(str) {
    let arra = text.splt(' ')
    let longest = ' '

    for (let i = 0; arr.length > i; i++) {
        let word = arr[i]
        if (word.length[i] > longest.length) {
            longest = word
        }
    }
    return longest
}